Copy-ready output templates for this pair.

| File | Purpose |
|---|---|
| `website-audit-scorecard-template.xlsx` | The real, working branded XLSX template. Generated by `../scripts/generate-scorecard-xlsx.py` (do not hand-edit; re-run the generator instead, per the template-injection discipline in `../research/distilled-audit-scoring.md` section 3). audit-scoring-worker-bee copies this file to `scoring/audit-scorecard.xlsx` in the shared audit workspace at the start of wave W7, then writes real leaf scores into the `Scorecard` sheet's leaf rows (never into a formula cell) before handing off to audit-reporting-worker-bee. Verified openable via `openpyxl.load_workbook` and via a LibreOffice headless recalculation pass at generation time - see `guides/01-rollup-procedure.md` for the verification transcript. |
| `leaf-finding.schema.json` | JSON Schema (draft-07) for the intermediate findings format every upstream Wave-5/W6 Bee writes and this Bee reads. A leaf finding that fails validation against this schema is rejected back to its `originating_bee` field per `guides/04-rejecting-a-leaf-finding.md`, never scored anyway. |
